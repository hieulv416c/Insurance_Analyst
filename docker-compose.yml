services:
  postgres:
    image: postgres:16
    container_name: postgres_dw

    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin123
      POSTGRES_DB: warehouse

    ports:
      - "5432:5432"